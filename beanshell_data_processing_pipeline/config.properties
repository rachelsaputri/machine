# Configuration for Beanshell Data Processing Pipeline

# Data source type: csv, json, default
dataSource=csv

# Transformation rule type: uppercase, filter_nulls, default
transformationRule=uppercase

# Validation schema type: not_empty, type_check, default
validationSchema=not_empty
